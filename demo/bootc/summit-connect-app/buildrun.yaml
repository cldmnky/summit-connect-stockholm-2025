apiVersion: shipwright.io/v1alpha1
kind: BuildRun
metadata:
  name: summit-connect-app-buildrun
  namespace: summit-connect-demo
spec:
  buildRef:
    name: summit-connect-app
  serviceAccount:
    serviceAccountName: shipwright-builder
  timeout: 30m
  # Optional timeout and params can be added here
